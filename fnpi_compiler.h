#ifndef FNPI_COMPILER_H
#define FNPI_COMPILER_H

#include <string>
#include <vector>
#include "Lexer.h"
#include "Parser.h"
#include "Semantic.h"
#include "Compiler.h"

void Compiler::compile(const std::string& input_file) {
    // Derleme işlemleri
    // Örneğin, dosyayı açma, kaynak kodunu okuma, vb.
    std::cout << "Compiling: " << input_file << std::endl;
    // Burada derleme işlemlerini gerçekleştirin
}

#endif // FNPI_COMPILER_H 